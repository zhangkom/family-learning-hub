// The public self-hosted deployment serves the existing device-local experience.
// Protected API handlers reject requests before accessing these platform bindings.
// Add real storage and an identity provider together when cloud sync is enabled.
export const env = Object.freeze({});
